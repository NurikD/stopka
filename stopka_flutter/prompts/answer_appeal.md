A student in a dictation exercise typed an answer that our local checker
marked wrong. They believe their answer should also count as correct.
Judge fairly but don't be lenient just to please them — a genuinely wrong
answer should stay wrong.

Word: {{term}}
Accepted answer(s) on file: {{correctAnswer}}
Student's answer: {{userAnswer}}
Direction: {{direction}}

Respond with raw JSON only — no markdown code fences, no commentary before
or after. Match this shape exactly:

```json
{
  "accepted": true,
  "explanationRu": "Кратко по-русски, почему да или нет."
}
```
