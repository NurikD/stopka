# Copy to passwords.yaml (never committed) and fill in. Serverpod loads it at
# start; values are read as session.passwords / Serverpod.instance.passwords.
# Production secrets live only on the server, not in git.

development:
  database: ''
  serviceSecret: ''

test:
  database: ''

production:
  database: ''
  serviceSecret: ''
  # Added in the AI-proxy stage:
  # geminiApiKey: ''
